rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all documents,
    // but only allow write access for authenticated users.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
